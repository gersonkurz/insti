#pragma once

// CLI uses the shared settings from the library
#include <insti/config/settings.h>

namespace insti
{
namespace config
{
    // theSettings and initialize_logging() are declared in <insti/config/settings.h>
}
}
